package com.example.employeepayrollsystem.models;

public class FixedBasedPartTime extends PartTime {
    public FixedBasedPartTime(int id, String name, int age, double earnings, int birthYear, int rate, float hoursWorked) {
        super(id, name, age, earnings, birthYear, rate, hoursWorked);
    }
}
